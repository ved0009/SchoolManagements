import { MenuDetailsPipe } from './menu-details.pipe';

describe('MenuDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new MenuDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
